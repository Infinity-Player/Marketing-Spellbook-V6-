# Frontend (Next.js)

Install:
  cd frontend
  npm ci

Dev:
  npm run dev

Build:
  npm run build

Notes:
- Theme toggling is mount-safe (applies on client mount).
- Many features are scaffolds; extend as needed.
